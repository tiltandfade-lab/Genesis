---
type: build-plan
status: staged 2026-07-02 midday — fires after batch 2 merges. Incorporates the midday audit (9 holes found + repaired; see BREACH §2e).
created: 2026-07-02
---

# Batch 3 — plan of record

## Units (stacked, sequential, same pipeline: Sonnet build → Opus review → Sonnet fix → honest gates)

1. **gap-wiring** ← THE AUDIT'S BIG FIND: TABLE-GAPS §7.2 wiring never had a unit while its
   tables shipped in wave 2a — and monster-tactics (BUILT) fires morale-flees with nothing to
   chase into. Scope: the chase gap-clock loop + `chase_start` trigger contract (flee resolved +
   player declares pursuit) · the Distant-Word distortion-lens binder (fact = a real ledger
   entry, dm holds the truth) · downtime intent + ledger roll + payout hooks · festival
   surfacing (a Textured+ drift row may chain to the festival table) · shrine/omen dressing lane
   + myth binding. THEN re-check the built tarot Majors' mutator refs for executability
   (guardrail: nearest-implementable was licensed; now the real systems exist).
2. **skin-grants-motifs** — SKIN-GRANTS.md in full (8 grant executors, skin-rolls-first, 14 motif
   kits [Fable anchors in the brief], tint-COMPOSES invariant, entrance beat, Blockwright
   palette hook) + the transformative re-author of the 3 skin tables + Grants/Motif columns.
3. **breach-core** — BREACH.md §1–§3 + §2e repairs: the 2d10 bell + frayMod, threshold/ambush
   entry, finale-exit rule, persistence d6 + stable-door map canon, marooned chained walks w/
   fixed debt, physics lenses (bounded — the four no-touch invariants), premiums + xpReport
   bucket, digest `activeWalk.realm`, prep suspension while marooned.
4. **breach-tables** — the 6 Breach/Nightmare d20s (12 approved samples verbatim as rows;
   The Interior as one Mythic ambush row per table w/ the §2e.6 assembly rule).
5. **outlandish-realms** — the d300 inventory scan → realm vocabulary (Adam skims) → `Realm` tag
   pass (tagging only) → the sourcing SUPERSEDE (§2e.3) → the ≥1-per-breach guarantee w/
   channel roll + fallback (§2e.7).
6. **realm-kits** — ~25 frame-mapped items × ~7 realms (~175): 12 mundane + 8 enchanted + 4
   wonders + consumables per realm; tech = charge items (§2e.4), rust-exempt (§2e.5); ammo
   scarcity as the take-home governor. PROVISIONAL, Adam skims. **+ ITEM RANKS (LOCKED,
   ADAM-REVIEW-1):** every realm item carries a 2–4 rung rank ladder w/ attunement level
   prereqs (active rank = highest rung reached; rides the built attunement machinery; the
   3-slot cap governs); the outlandish-realms unit adds ladders to the d300's high-power +
   reality-breaking rows; reality-breaking rungs floor at L9 ALWAYS. **+ the RANK-SMITH:**
   tinker NPC role (Urban Fabric kit + breach-touched rider) wakes the next rung for gp +
   (high rungs) a component job-walk. Three Fable anchor ladders in the unit brief; the
   Jordans ladder is canonical.
7. **safety-guard** — the content-safety compile gate (§2e.9: denylist file + scrub-guard
   extension, fails compile) + the DM-CHARTER addendum (real-world slurs banned absolutely;
   fictional-peoples prejudice licensed, NPC-voiced only) — the charter edit is frontier prose,
   orchestrator lands it.
8. **touched-npcs** — the fray-scaled (~2%→8%) breach-touched rider d12 on `rollNPC` + the
   Distant-Word/TIYL lore links.
9. **urban-fabric** — `docs/URBAN-FABRIC.md` (verified gap 2026-07-02: typed buildings absent;
   Tavern 2.0/Settlement 1.1 robust but vault-only; no city structure): the type-kit roller
   (~12 kits, proprietors always rolled), the Tavern 2.0 extraction pass (tables out verbatim,
   procedure archived), lazy district mint by tier, `gen interior opts.type`, the tavern as the
   distant-word/downtime/lodging surface.
10. **wiring-sweep-A** — `docs/WIRING-MAP.md §B Wave A` AS REVISED by Adam 2026-07-02 pm:
    **`docs/JOB-WALKS.md`** (tier-scaled postings mint 1–3 segment walks; unclaimed postings
    resolve without you) · the SEED DISPATCH (catalyst/rumor-intel → prep + TIYL seeding, with
    the **≥1 non-urban seed anchor guarantee** — adventures start in the middle of nowhere too)
    · travel lanes w/ up-to-par craft gate · **REST-RISK** (security-class model: paid beds buy
    real safety; Volatile+ can interrupt the long rest; table fixed + expanded to d100
    PROVISIONAL) · parley-wants→social · behavior-if-hunted→huntRules/chase · **if-ignored→
    World-Turn escalation** (Adam: "no house still on fire 50 days later" — time-honesty is the
    product) · interactables→walk object lanes · region-encounter header fix. One harness
    `dev/verify-wiring-a.mjs` asserting every wired table actually FIRES from its seam + the
    seed-locus distribution check.
11. **wiring-sweep-B + hygiene** — WIRING-MAP §B Wave B (puzzle chain, the catalyst/background/
    spectacle dispatch map, environment-skin→district skin, place-* depth, blessings/charms lane,
    wilderness magic/art parity, empty-result texture lane, camp cooking, urban-boss apex,
    door/exit dressing) + §B reconciles (morale-outcome table replaces the built d6 disposition;
    mythic-lenses convergence check; legacy travel-* archived) + §C hygiene (registry regen, stub
    dispositions, malform fixes) + **ADAM-REVIEW-1 absorptions:** the Place-Drift `Effect`
    column tag pass + `applyDriftEffect` executors (§1 vocabulary — clock±/npc-swap/thread/
    price±/stock/contact/festival/board/rep/codex-only/none; existing machinery only) ·
    the region NAME-per-world generator fix (identity row = character+vector; name from
    culture/pattern banks) · the `dwalkCoin` low-tier retune to a 1 gp mechanical minimum
    (pure-GP ruling; copper stays narration).

15. **spice-reband** (from Adam's corpus verdict + `docs/SPICE-RULER.md` — runs EARLY, before
    the other authoring units so they inherit honest examples): the eleven wave-1/2a d100s —
    relabel existing rows to their honest band (text verbatim, labels only, diff-audited) +
    author ~15–20 new hot-tail rows per table to the ruler. The ruler is J0-law for ALL batch-3
    authoring.

## Post-batch-2 review checklist (verify during the batch-2 gate re-run, BEFORE batch 3 fires)

- World-Turn arrival drift fires on TRAVEL's `walk_complete` arrival (T3 × travel interlock).
- The `recall` kind + lull `echo` actually built in the world-turn unit (WORLD-TURN §5).
- Tarot Majors' mutators reference only built systems (else gap-wiring re-check catches).
- Reputation's fade hooked `worldTurn` correctly (guardrails H3 interlock).
- Companions' morale-loyalty modifier composes with monster-tactics' morale (both built
  separately — same roll site?).
- The battlemap's `move_zone` OA rule vs travel walks' clock advance (no event collisions).
- verify-digest-diet's flaky <12KB guard (~1.7%) — tune fixture or threshold while in there.

12. **dm-eval** (RISK-REGISTER R5 — allowed under the freeze: protection, not expansion) — a DM
    behavior regression suite: ~10 fixture turns (combat, social, walk, arrival-drift, morale,
    a branch, a gen tease, a lull) scored against the CHARTER + runbook rules (never rolls
    player dice · narrates FROM rolls · no coaching · budget · second-person-is-PC-only ·
    honors binding mechanics). Runs on every model change + before any prose edit lands.
    `dev/dm-eval/` fixtures + a scoring script; thresholds provisional until baselined.

13. **forever-guards** — FULL SPEC: `docs/FOREVER-STORAGE.md` (R9; measured walls: main-thread
    jank in WEEKS at current saveU behavior, quota death at ~40–60 total sessions — the
    fortnight burns ~25% of runway, so this lands WITH batch 3, before play). IDB migration w/
    LS-original preserved one release · changed-world-only debounced saves · quota wrap +
    storage meter + export-on-failure · dmlog archive lifecycle (ledger whole forever) · backup
    nudge · **vintage fixture #1 frozen from the POST-batch-3 format** (deliberately LAST in the
    chain so the fixture captures every unit's state shape) + `verify-migrations.mjs` ·
    `ATTRIBUTION.md` (SRD 5.2.1 CC-BY + Ironsworn/Starforged CC-BY, consolidated). **LOCKED
    (Adam): IRONMAN ALWAYS** — no checkpoints ever; the escape valve is the diegetic RETCON
    NEGOTIATION (player asks, DM adjudicates, pre-consequence scope only, never death, logged
    as an `adjudication` ledger entry — undo itself is captured, never silent). Retcon protocol
    prose rides the batch-3 frontier-prose landing (DM-CHARTER register).

## Standing rules (unchanged)

BATCH-GUARDRAILS G0/G9 + BATCH2-GUARDRAILS H-register apply; approved samples are voice LAW and
land verbatim; PROVISIONAL callouts on all authored content; explicit-path commits; no pushes;
gates re-run personally by the orchestrator before any merge.
