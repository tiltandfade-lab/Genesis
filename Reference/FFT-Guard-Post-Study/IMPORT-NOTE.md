# Import note — 2026-07-24

Imported from `~/Desktop/Genesis FFT Guard Post Study/` (Codex + Adam working study,
2026-07-23) on Adam's instruction. The Desktop copy was deliberately kept outside git so it
would survive the 2026-07-23 repository/container relocation; the relocation completed the
same evening and the fold-in was never executed — this import discharges it.

Imported: the three root docs, `analysis/*.md`, the cohort overview PNG, and
`reference-index/`. The companion working catalog is imported verbatim as
`DESKTOP-CATALOG-SNAPSHOT.md` in this folder (its internal links still point at the
Desktop paths). **Authority resolution (consolidation close 2026-07-24):** the door-lane
session had already folded the packet into a structured repo authority —
`docs/GOLDEN-SITES-CATALOG.md` — on 2026-07-23; that file is the living catalog
authority. The snapshot here is the full verbatim Desktop record preserved for
provenance; do not edit it.

**Deliberately NOT imported (remain Desktop-local; heavy binaries, no-LFS posture):**

- `material-maker-1.3/` (215 MB — the pinned app copy; canonical install is
  `/Applications/Material Maker 1.3.app`, sha pinned in MATERIAL-LANE.md)
- `tools/` (37 MB), `downloads/` (26 MB), `maps/` (31 MB),
  `historical-reference/` (26 MB), `analysis/contact-sheets/` (7 MB),
  `analysis/guard-post-cohort/` (2.3 MB)

If any of those need repo residency later, that is a deliberate Adam decision, not a
default. The lock audit (`analysis/GUARD-POST-LOCK-AUDIT.md`) is the authority snapshot
distinguishing LOCKED founder rulings from WORKING defaults and OPEN choices as of
2026-07-23; `docs/STRUCTURE-KIT-CATALOG.md` is the in-repo spec that consumes it.
