---
type: review-notes
status: Adam's first PROVISIONAL-table review pass (2026-07-02 evening) + Fable's Place-Drift mechanization scan. BINDING inputs to batch 3.
created: 2026-07-02
---

# Adam Review 1 — table verdicts + the drift-effects ruling

## §1. Place Drift → the `Effect` column (the Grants pattern, third application)

Adam's principle: drift rows that IMPLY state change should CAUSE it mechanically where an
existing mutator fits; DM-narrate+codex-capture where that's genuinely faster; never a new
system. Fable's 100-row scan classified (counts approximate, tag pass finalizes):
`clock±` ~15 (the Textured faction band moves REAL faction/pressure state) · `npc-swap` ~8
(old holder gets a life-event; `rollNPC` mints the successor) · `thread` ~12 (Strange rows
also flag `needsEffectDie`; row 100's hill anchors a doom BEARING) · `price±`/`stock` ~9
(existing economy tilt) · `contact` ~6 (ambient pool +1) · `festival` 2 (chain-roll Festival) ·
`board` 1 (row 16 = JOB-WALKS posting refresh) · `rep` 3 (row 13 = the Distant-Word lens
pointed at yourself; row 79 = a renown consumer) · `codex-only` ~24 · `none` ~12.
**Build:** the `Effect` column tag pass (Sonnet, closed vocab, Adam skims) + `applyDriftEffect`
executors (each ≤15 lines, existing machinery only) → fold into batch-3 **wiring-sweep-B**.
Lazy-on-arrival already guarantees no wasted rolls (every fired row is in front of the player).
The `arrivalBrief` is the DM's adventure-food; on-the-fly canon captures as always.

## §2. Verdicts + rulings from the pass

- **Downtime trigger (Adam asked):** player-initiated — the week-pass at an inhabited node
  offers the intent picker; script rolls; DM narrates from digest. (Already the gap-wiring
  design; confirmed.)
- **Region Identity — GOOD, with the name-collision fix:** identity rows keep CHARACTER +
  vector; the NAME generates per-world from culture/pattern banks (same soul, new name each
  universe). → batch-3 wiring-sweep-B (small; regions unit is merged, this is a follow-up).
- **Name Cultures: approved ("excellent work").**
- **Festival chain from drift: approved (Adam spotted the wiring).**
- **CURRENCY RULING: pure GP** (BG3-style; already implemented). Fix the inconsistency: retune
  `dwalkCoin`'s low-tier cp/sp formulas to a **1 gp minimum** mechanical yield; copper/silver
  stays as narration color only. Valuables gp values sane vs anchors — no change.
- **Outlandish:** Band column ✓ done; Realm/genre column = batch-3 (thinny draws filter by
  realm); ~175 new items arrive as realm kits (the d300 itself is tagged, not expanded).
- **THE TIER RULING — LOCKED (Adam, 2026-07-02 evening): ITEM RANKS, the Diablo answer.**
  Every outlandish/realm item carries a rank ladder (2–4 rungs, each with an attunement level
  prerequisite); active rank = highest rung the attuner's level reaches. The full ladder IS the
  item's inherent nature — what varies is how much wakes. Canonical example (Adam's):
  `Jordan 1s — R1(L1) +1 ft jump · R2(L4) +10 ft speed · R3(L7) ignore fall damage ·
  R4(L10) fly 6s, 1/day`. Rides the BUILT attunement machinery; the standard 3-attunement-slot
  cap is the free balance governor. Authoring: bespoke ladders on high-power + reality-breaking
  bands + all realm-kit items; utility/combat = 1–2 rungs or flat. Surfacing gates (L4 bands +
  breach-only top band) UNCHANGED underneath. **THE RANK-SMITH:** a rare NPC role ("one who
  knows the make") wakes the next rung early — gp price + (high rungs) a component that mints a
  JOB-WALK; hard floor: reality-breaking rungs never wake below L9. Slots into Urban Fabric's
  tinker kit + the breach-touched rider. → batch-3 realm-kits + outlandish-realms absorb the
  rank schema; three Fable anchor ladders ride the unit brief.
- **THE BASTION (Adam's legacy-vault vision):** v0 works TODAY emergently (persistent world +
  codex houses + item instances = stash-and-inherit is already possible; successors can loot
  the ancestor's rented room). The FORMAL system (stronghold, facilities, staff — 2024 DMG has
  a Bastion system to draw from) = NEW SYSTEM → PARKED ★ beside the Crowning (the legacy vault
  and the campaign ending are one design conversation, post-fortnight).
- **Dungeon Dressing → Blockwright prop mapping:** the mega table's noun classes map onto the
  `bwFeature` kit — queued as a blockwright follow-up (post-verdict art lane).
- Walk Skins: review HELD by design (batch-3 re-authors them).

## §3. Standing note

This file is the template for review passes 2..N: Adam dumps; Fable scans/classifies/rules;
verdicts land here; batch plans absorb. The PROVISIONAL flag comes off a table only when its
pass is logged here.
