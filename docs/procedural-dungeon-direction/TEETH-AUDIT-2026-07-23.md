---
type: audit
status: PROPOSED dispositions (Fable gate session, 2026-07-23, under THE TEETH LAW) — Adam's
  review disposes; every future pass spec MUST cite its rows here and land NEEDS-TEETH items
  in the pass that owns them
---

# Teeth Audit — Stage-1 binding laws vs. enforcing checks (28 need · 20 have · 9 stay prose)

Read-only audit. Scope: `Genesis-q12b/docs/procedural-dungeon-direction/wave-12/SLICE-1-GATE-MATRIX.md`,
`Genesis-q12b/docs/C1A-CLAY-ROOM.md` (incl. D12–D14), `Genesis/docs/FEATURE-PRIORITIZATION.md` §0/§3,
founder riders at wave-07 §16.2/§16.6/§16.7 (P7.1/P7.5/P7.8), wave-09 §18.2/§18.6 (P9.1/P9.10 + the
F9.2 risk-rider exchange), wave-11 §19.2/§19.6 (P11.1/P11.2/P11.4), wave-12 §20.2/§20.6/§20.9
(P12.1/P12.2 + the fired riders), `Genesis/docs/GRAPHICS-CONVERGENCE-CHARTER.md` §1/§2, and
`Genesis/CLAUDE.md` Disciplines (cross-reference only). Existing-check inventory cross-referenced:
`ls dev/verify-*.mjs`, `ls build/*.py`, `.github/workflows/ci.yml`.

**Grounding note (not required by scope, but load-bearing for this audit's accuracy):** C1A is
already implemented on a separate worktree/branch (`Genesis-c1a`, `feat/c1a-room-truth`,
`dev/verify-clay-room.mjs` + `src/engine/clay-room.js`). That harness's committed state (as of
`d1c56bfa`) has 11 checks (determinism, truth shape, BodyForm provenance, prose twin, light-profile
shape, refusal, renderer-zero-mechanics, telemetry leak, check-manifest, boot self-mount, door-state
fact). It also currently carries a **local, uncommitted** working-tree diff (`git status` shows
`modified: dev/verify-clay-room.mjs`, +139 lines, checks 12–14g) that drafts grep-gates for D12a
(seam grid) and D12b (door-wiring), exactly the two redlines Adam issued after capture packet #1
caught the stubbed door. This is the Teeth Law in action mid-repair: the fix is written but not yet
landed. D13 (provenance audit) has no check at all yet, drafted or committed. This is reflected in
the rows below.

---

## NEEDS-TEETH (the deliverable)

| Law (short) | Source | Disposition | Existing or proposed check | When |
|---|---|---|---|---|
| Provenance audit: every object in the clay scene graph carries `{builder, recordRef}`; harness lists any ownerless mesh and FAILS on one — generalizes to every later clay pass | C1A-CLAY-ROOM.md D13 · GRAPHICS-CONVERGENCE-CHARTER.md §1 #6 | NEEDS-TEETH | No check exists, drafted or committed — `dev/verify-clay-room.mjs`'s uncommitted diff stops at check 14g, before a check 15 for this. Proposed: **runtime-assert** in `dev/verify-clay-room.mjs` (extended to a shared helper later harnesses inherit) that walks the mounted THREE scene graph under the clay-room host and asserts every `Object3D`/`Mesh` carries `userData.builder` + `userData.recordRef`, failing by name on the first orphan. This is the single highest-priority gap — the generalized version of the exact defect class the stubbed door was. | C1A (lands with the wiring-audit re-wire unit, per the doc's own words) |
| THE DOOR CONTRACT hinge geometry: "extruded rectangle... hinges outward on a fixed axis on one of its corner vertices; double doors hinge on opposite sides, mirroring each other" | C1A-CLAY-ROOM.md D12b | NEEDS-TEETH (half-covered) | The uncommitted draft checks 14a–14g grep-gate that the door routes through the production builder and never hand-rolls `ExtrudeGeometry`/`THREE.Shape` — that proves *no bespoke geometry*, not that the hinge *behaves* per the contract (fixed corner-vertex axis, mirrored on doubles). Proposed: **runtime-assert** reading the built door leaf's rotation pivot/transform at open vs. closed state, or a **capture+golden** pair (closed vs. cracked-open frame) diffed against an Adam-approved reference. | C1A (state) → C1B (the typed door catalog, D14, is where multiple hinge configurations actually need proving) |
| D12a seam grid: semi-transparent overlay (opacity 0.25–0.35), derived from `record.dims`, never hardcoded counts | C1A-CLAY-ROOM.md D12a (Adam, capture packet #1) | NEEDS-TEETH | **Already drafted, uncommitted** in `Genesis-c1a`'s working tree: `dev/verify-clay-room.mjs` checks 13/13a–13g (grep-gate on the additions region for a `clayRoomBuildSeamGrid(record)` function, dims-driven loop bounds, `THREE.LineSegments`, an opacity constant in range, and a sane `renderOrder`). Action: land it — commit + prove green + fold into the harness's standing count. | C1A (before U-close) |
| D12b legible door: production door-builder wiring, no bespoke/stub leaf | C1A-CLAY-ROOM.md D12b (Adam's redline — the literal Teeth Law origin story) | NEEDS-TEETH | **Already drafted, uncommitted**: `dev/verify-clay-room.mjs` checks 14/14a–14g (archetype `"door"` registration, state read from `record.portal.state` not a literal, `kitDoors`/`widthAxisIsZ` wiring, negative checks banning `ExtrudeGeometry`/`THREE.Shape` in the additions region, doorframe-literal-shape check). Action: land it. | C1A (before U-close) |
| Preview-sandbox / world-tier isolation: the C1A surface "never reads or writes `U` / world saves" | C1A-CLAY-ROOM.md D10 · P11.4 (preview sandbox flag) | NEEDS-TEETH (half-covered) | `record.tier === "test"` and `Object.isFrozen(record)` ARE checked (`dev/verify-clay-room.mjs` 2e/2f), but there is no grep-gate confirming the module/additions region contains no `loadU`/`saveU`/`U.` reference — the "never reads or writes" half is currently true only by code-review inspection, not by an automated check. Proposed: **grep-gate** alongside the existing telemetry-leak scan (checks 8a/8b), same file, same convention. | C1A |
| Clay-lighting rider's "full native MacBook resolution" + effective ambient in the ACTUALLY-mounted scene | C1A-CLAY-ROOM.md D4 · wave-12 §20.9 (Adam's answer 1, verbatim) | NEEDS-TEETH | The light-PROFILE data shape (two temps, opposing sides, ambient ≤0.25 *as authored*) is checked (`dev/verify-clay-room.mjs` 5a–5d). The rendered claim — canvas backing store = device pixels, DPR applied, effective (post-floor-clamp) ambient — is currently a **capture-packet, human-counted** line ("renderer size `<w>×<h>` @ dpr `<n>`") per the gate matrix's own words ("countable packet checks"). Proposed: a **runtime-assert** (headless-Chrome, precedent: `dev/verify-light-lab.mjs`'s "measured off the ACTUAL rendered frame" convention) reading `canvas.width/height` vs. `window.devicePixelRatio` and the resolved ambient after `STAGE_AMBIENT_FLOOR` clamping. | C1A close |
| Reopen trigger: a build-reality contradiction with a closed wave's ruling must be recorded as a named contradiction in a dated section **before** code lands on that subject | SLICE-1-GATE-MATRIX.md §7 · wave-12 §20.8 item 8 | NEEDS-TEETH (mechanical half only — the judgment half correctly stays prose, see STAYS-PROSE) | No mechanical check that a diff touching a closed-law-governed path carries a "REOPENS: wave-N §X" tag. Proposed: **grep-gate** (pre-merge) scanning the branch's commit messages / CHANGELOG entry for a REOPENS tag whenever the diff touches paths a small manifest marks as closed-law-governed. Weak precision by design — it only enforces that a reopening got *recorded*, not that the underlying judgment was sound. | Repo tooling — best done at a clean close |
| World-tier leak (general): live/test/experimental never leak into one another at the canonical save boundary | SLICE-1-GATE-MATRIX.md standing gate · P12.1 (wave-11 §19.6 item 4 rider, bound at wave-12 §20.6 item 1) | NEEDS-TEETH | The C1A *instance* of this (record.tier fixed to "test") has teeth (see HAS-TEETH). The general save-boundary schema P12.1 describes (tiers as first-class, non-leaking, bound into every canonical record) does not exist yet — P12.1/P12.2 are unbuilt. Proposed: **jsdom-harness** cloned from `dev/verify-storage.mjs`'s fake-IDB convention, asserting a tier field on every persisted record type and a save-path guard that refuses to write a live-tier record from a test/experimental harness run. | C1F |
| Budget measurement: harness wall-time + memory on the Mac target recorded in the pass record (WARN — existence is the BLOCK evidence, threshold warns) | SLICE-1-GATE-MATRIX.md standing gate §3 item 8 | NEEDS-TEETH | No `dev/verify-*.mjs` records wall-time or memory anywhere (confirmed by grep: only `verify-geometry-fixtures.mjs` touches `performance.now`, for an unrelated purpose). Proposed: a small **python-validator or node wrapper** (`dev/measure-pass-budget.mjs`) that times a pass's harness run and captures `process.memoryUsage().rss`, appending to a versioned budget log per pass. Never blocks (WARN); its *existence* is what the standing gate actually requires. | C1A (retrofit), then every pass |
| Capture set: fixed-camera captures archived per pass (existence = BLOCK evidence; quality = WARN) | SLICE-1-GATE-MATRIX.md standing gate §3 item 9 | NEEDS-TEETH | A capture rig (`dev/capture-clay-room.cjs`) was drafted and committed on the **docs** worktree (`Genesis-q12b`, commit `a0e32f82` "C1A capture packet #1") but does **not** exist in the **code** worktree (`Genesis-c1a`) — confirmed absent there. The capture instrument that produced packet #1 (which caught the stubbed door) is not yet a standing, re-runnable part of the code lane. Proposed: **capture+golden** — land the rig into the code worktree, with output paths retained under a capture corpus directory. | C1A, then every pass |
| Fixture retention: a pass's fixture + expected trace enter the retained corpus; no later pass overwrites them | SLICE-1-GATE-MATRIX.md standing gate §3 item 2 | NEEDS-TEETH | No registry analogous to `manifest.json` exists for retained fixture paths/hashes; nothing currently stops a later pass's commit from silently deleting or mutating an earlier pass's retained fixture. Proposed: **python-validator** (`build/check-fixture-retention.py`) maintaining a small path+hash registry, diffed at CI time, refusing silent drift (an explicit "supersede" tag would be the escape hatch). | Before C1B (once ≥2 passes' fixtures coexist, the risk becomes real) |
| Migration rider: save schema v1 makes the migration registry live; every later schema change carries its expansion fixture; migrations always BLOCK on their fixture | SLICE-1-GATE-MATRIX.md §8 · P12.3 (wave-12 §20.2) | NEEDS-TEETH | No save schema exists yet, so no migration registry exists. Strong precedent to clone: `dev/verify-migrations.mjs` already does exactly this shape for FOREVER-STORAGE's current localStorage→IndexedDB migration. Proposed: **jsdom-harness** cloned from that convention for save-schema v1, plus a **python-validator/grep-gate** that fails if a schema-version bump lands without a matching new `old-save-vN` fixture in the same commit. | C1F (when schema v1 lands) |
| DC reveal-at-commit: no number pre-roll (qualitative read + licensed stakes only); DC shown at commitment with the roll; per-player setting defaults to reveal-at-commit | wave-07 §16.7 closure (Adam, verbatim "yep, sounds great") · SLICE-1-GATE-MATRIX.md C1B row · wave-12 §20.9 rider list | NEEDS-TEETH | CheckContract doesn't exist yet. Proposed: **jsdom-harness** extending `dev/verify-check.mjs` (which already proves the pure margin-ladder math) to assert the pre-roll payload never carries a `dc` field and the post-roll receipt does, plus a per-player toggle test. | C1B |
| CheckContract failure-license fields: on failure, the engine selects only from pre-declared licensed complication families; no un-narrated silent complications; never secret success | P7.5/P7.8 (wave-07 §16.2, §16.6 crit carve-out) · SLICE-1-GATE-MATRIX.md C1B/C1D rows | NEEDS-TEETH | Not built. `dev/verify-check.mjs` proves margin math only, not licensing. Proposed: **jsdom-harness** on the new CheckContract module: (a) failure resolution draws only from the declared license list, (b) an out-of-license complication attempt is refused/throws, (c) a provider narrating a fact the engine never committed is caught at the seam (ties to the digest's fact-lock check below). | C1B (movement checks) / C1D (combat) |
| ActionIntent/ActionReceipt: one schema is the single combat-and-crisis contract; explicit payment sources (no auto-Dash); typed target domains | P7.5 (wave-07 §16.2) · SLICE-1-GATE-MATRIX.md C1C/C1D rows | NEEDS-TEETH | Not built (`grep -rl "ActionIntent\|ActionReceipt" src/engine` returns nothing). Proposed: **jsdom-harness + grep-gate** extending `dev/verify-combat-actions.mjs`/`dev/verify-combat.mjs`'s convention, asserting every action-resolving path constructs exactly one intent/receipt shape (a code-sweep against duplicate resolvers). | C1D (MVP gate C1E) |
| TacticalQueryKernel: one pure query brain for reach/path/LOS/cover/fit; no consumer computes its own answer | P7.1 (wave-07 §16.2) · FEATURE-PRIORITIZATION.md §3 row 2 ("runtime assert + code sweep prove no consumer computes its own answer" — the test column names its own check type) | NEEDS-TEETH | Not built. The test column already specifies the check shape, just not implemented. Proposed: **golden ray/path jsdom-harness** for C1B, then a **grep-gate/code-sweep** at C1D scanning for any independent reach/path/LOS computation outside the kernel module. | C1B (fixtures) → C1D (code sweep across all consumers) |
| Illegal intents refused honestly (typed refusal, never silence); no prose-committed outcome | SLICE-1-GATE-MATRIX.md C1C row | NEEDS-TEETH | Depends on ActionIntent existing first (above); folds in as a specific assertion on that same harness (typed refusal object, not a thrown generic error or silent no-op). | C1C |
| Save/load/interrupt: identical terminal truth across skip/pause/save/load; crash mid-write reconciles deterministically via two-phase receipts | P12.2 (wave-12 §20.2) · SLICE-1-GATE-MATRIX.md C1F row · FEATURE-PRIORITIZATION.md §3 row 5 | NEEDS-TEETH | Not built, but `dev/verify-storage.mjs` + `dev/verify-migrations.mjs` + `dev/verify-idb-hydration.mjs` (fake-IDB shim convention) are the exact template. Proposed: clone that convention for the new chunk-snapshot + receipt-log model, adding a crash-mid-write simulation (kill the fake-IDB write mid-transaction) asserting prepared-without-commit rolls back and committed re-applies idempotently. | C1F |
| Hand rebuilds identical from receipts (never stored, always rebuildable — F9.1) | wave-09 §18.6 item 13 · SLICE-1-GATE-MATRIX.md C1F row | NEEDS-TEETH | Same C1F harness as above; a distinct assertion (rebuild the hand from the receipt log twice, byte-compare). | C1F |
| Hand floor: the DM's hand may never be empty — LOCAL SPICE/quiet-beat/walk-generated material guarantee a non-empty hand from the first room | wave-09 F9.2 risk-rider #2 (Adam, verbatim "We need to ensure that there are always cards in the hand") · SLICE-1-GATE-MATRIX.md §5/C1G row | NEEDS-TEETH — **and a scheduling gap worth flagging**: the gate matrix binds this at C1G, but the mechanism that would supply hand content (the full P9.1 card registry) is scheduled at Stage 4 (C2B/C4C) in FEATURE-PRIORITIZATION §3 row 4 / §6. Only a *minimal* C1G instance (LOCAL SPICE + quiet-beat supply) is plausibly in scope now; neither the minimal nor the full version has a check. | Proposed: **property/fuzz-style jsdom-harness** (loop-until-crossed per the repo's own seeded-RNG convention) asserting hand-assembly length ≥1 across a battery of seeded "starved" scenarios (no active crises, no obligations, fresh world). | C1G (minimal instance) / C2B–C4C (full registry) |
| Performance layer never carded, never scheduled, never scored | wave-09 F9.2 risk-rider #1 (Adam, verbatim) · SLICE-1-GATE-MATRIX.md C1G row | NEEDS-TEETH | DM seat/digest schema unbuilt. Proposed: a **runtime-assert/schema check** that the digest's typed fields simply have no slot for performance/voice/expression as a scored or scheduled item, plus the transcript-review already named as the P9.10 MVP-gate mechanism (see FEATURE-PRIORITIZATION.md §3 row 4). | C1G |
| No menus, no coaching; legal actions surfaced only as families, never enumerated picks | P9.10 (wave-09 §18.2) · Charter §3 bars (referenced, not in scope to re-quote) · SLICE-1-GATE-MATRIX.md C1G row | NEEDS-TEETH | Digest schema unbuilt. Proposed: **shape check** on the digest object (legal-action field is a family enum, never a per-target enumerated list) plus a **grep-gate/transcript-scan** for coaching language patterns in seat/fallback output. | C1G |
| Fact-locked prose: a provider cannot substitute a different consequence than the one the engine committed | P7.8/P9.10 · SLICE-1-GATE-MATRIX.md C1B/C1D/C1G rows | NEEDS-TEETH | Not built, but well-specified already — FEATURE-PRIORITIZATION.md §3 row 4 names the exact check: "provider-swap test: same facts, same refusals, same committed consequence when prose is timely, late, unavailable, or invalid." Proposed: **jsdom-harness** swapping mock providers against one fixed digest, diffing committed state across swaps. | C1G |
| Card registry anti-drift law: a card with no owner receipt cannot exist | P9.1 (wave-09 §18.2) | NEEDS-TEETH | Not built; scheduled at Stage 4 per the P9.1 disposition's own "Clay: C2B (promise/payoff) then C4C" line — outside this slice's C1A–C1G span, flagged here only because the gate matrix's hand-floor rider (above) implicitly depends on some hand-content mechanism existing sooner. Proposed: **runtime-assert** — every card object in the registry carries a validated owner-receipt reference; construction without one throws. | C2B |
| Door catalog: a typed library (~10 door types) placed at graphed positions; connection record owns placement/state; renderer is pure projection | C1A-CLAY-ROOM.md D14 (Adam's door-review framing, verbatim) | NEEDS-TEETH | Explicitly deferred to C1B in the doc itself; nothing built. Proposed: **python-validator** compiling a declarative door-catalog source (markdown/YAML, per P11.3's proven pattern) into a runtime registry, plus a **jsdom-harness** asserting every placed door resolves to a catalog entry and the renderer never invents geometry outside it. | C1B |
| Accessibility/prose-twin completeness — generalized across passes (not just C1A) | GEN-LAW-3/TEXT-FIRST doctrine · SLICE-1-GATE-MATRIX.md standing gate §3 item 5 | NEEDS-TEETH (partial — C1A instance has teeth) | `dev/verify-clay-room.mjs` check 4/12 prove this for C1A specifically, but the assertion logic is hand-written inline, not factored into a reusable helper later passes (C1B–C1G) can call without reinventing it. Proposed: extract `assertProseTwinCompleteness(record, prose)` into a shared harness utility before the next pass ships its own prose twin. | C1B |
| Renderer-owns-zero-mechanics — generalized and raised in stakes once real dice/pathing/combat exist | SLICE-1-GATE-MATRIX.md standing/per-pass gates | NEEDS-TEETH (partial — C1A instance has teeth) | `dev/verify-clay-room.mjs` check 7 greps for `d20\|roll\|applyEvent\|attack` tokens in a mechanics-free room — trivial to keep green when there's no mechanics to leak. The real test comes at C1B–C1D once the renderer sits next to live pathing/combat resolution; no check yet proves "single resolution path" or "renderer/DM prose never decide success" at that scale (see the ActionIntent/TacticalQueryKernel rows above, which are this law's real teeth). | C1B–C1D |
| Machine-diffed capture goldens (no-human production contract; front/back-end gate split) | GRAPHICS-CONVERGENCE-CHARTER.md §1 #8 · wave-12 §20.9 P12.12-R2 (Adam-approved captures become machine-diffed goldens) | NEEDS-TEETH | Explicitly named as the intended mechanism but not yet built — captures currently require Adam's manual eyes-on approval every time, with no automated regression-diff once a capture IS approved. Proposed: **capture+golden** — freeze an Adam-approved capture as a baseline (perceptual-hash or pixel-diff), then gate later renders against it within tolerance, without requiring a fresh human eyeball every time nothing changed. | After C1A's first Adam-approved capture |

## HAS-TEETH

| Law (short) | Source | Disposition | Existing check (exact file) | When |
|---|---|---|---|---|
| No parallel room compiler; manifest is the module/symbol index; no orphan modules | C1A-CLAY-ROOM.md D6 · P11.2 (INHERITED) · CLAUDE.md "edit-source → compile-artifact" | HAS-TEETH | `build/check-manifest.py` (also run as `dev/verify-clay-room.mjs` check 9) | C1A (standing, every module edit) |
| Replay determinism: same seed + intents → byte-identical state on a fresh run | SLICE-1-GATE-MATRIX.md standing gate #1 (C1A instance) | HAS-TEETH | `dev/verify-clay-room.mjs` checks 1a/1b | C1A |
| Canonical room truth shape: exact cells/walls/one portal, stable ids, frozen, versioned | C1A-CLAY-ROOM.md (record shape) · SLICE-1-GATE-MATRIX.md C1A row | HAS-TEETH | `dev/verify-clay-room.mjs` checks 2a–2g | C1A |
| BodyForm provenance strictly equals the registry; loud failure, never a silent default | C1A-CLAY-ROOM.md D8 | HAS-TEETH | `dev/verify-clay-room.mjs` checks 3a–3d (includes a mutation check — satisfies CLAUDE.md's "7th point" rubric) | C1A |
| Prose twin renders every canonical fact, incl. the door-closed clause | C1A-CLAY-ROOM.md D9, D12b (fact half) | HAS-TEETH | `dev/verify-clay-room.mjs` checks 4, 12 | C1A |
| Clay-lighting rider — profile DATA shape (two temps, opposing sides, ambient ≤0.25 as authored) | C1A-CLAY-ROOM.md D4 | HAS-TEETH (data shape only — see NEEDS-TEETH for the rendered-scene half) | `dev/verify-clay-room.mjs` checks 5a–5d | C1A |
| Workbench floor v0 edit-refusal returns the typed D11 shape | C1A-CLAY-ROOM.md D11 · P11.5 (Explain panel pattern) | HAS-TEETH | `dev/verify-clay-room.mjs` check 6 | C1A |
| Renderer-owns-zero-mechanics (C1A instance, no real mechanics present yet) | SLICE-1-GATE-MATRIX.md C1A row | HAS-TEETH (see NEEDS-TEETH for the generalized/higher-stakes version) | `dev/verify-clay-room.mjs` check 7 | C1A |
| Telemetry/privacy leak: zero `fetch`/XHR/WebSocket in new C1A files | SLICE-1-GATE-MATRIX.md standing gate #4 (GEN-PROD-7/Q12-A) | HAS-TEETH (C1A instance) | `dev/verify-clay-room.mjs` checks 8a/8b | C1A |
| Dev-flag isolation + boot self-mount (D1/D1a) | C1A-CLAY-ROOM.md D1 | HAS-TEETH | `dev/verify-clay-room.mjs` checks 10/10a–10c | C1A |
| Portal/door state = closed (the fact, not the geometry) | C1A-CLAY-ROOM.md D12b (fact half) | HAS-TEETH | `dev/verify-clay-room.mjs` check 11 | C1A |
| Rights audit: fixture content is SRD 5.2.1/original only, no WotC Product Identity | SLICE-1-GATE-MATRIX.md standing gate #6 | HAS-TEETH (at the corpus level clay-room's registries already inherit) | `build/scan-creature-ip.py`, `build/scan-ip-remaining.py` | Standing |
| CI runs check-manifest + the full verify-*.mjs battery + bridge + table-lint + bug-probes on every push/PR | Arrival-law "fresh deterministic green" half (FEATURE-PRIORITIZATION.md §0) | HAS-TEETH (proves the fixture battery is green — does NOT satisfy "human eyes on actual output," see STAYS-PROSE) | `.github/workflows/ci.yml` | Standing |
| Margin-ladder / DC math (precursor to CheckContract's failure-license law) | Precursor to P7.8 | HAS-TEETH (today's mechanic, not yet the new contract) | `dev/verify-check.mjs` | Standing (template for C1B/C1D work) |
| Single orchestration seam wiring the current combat stack into `applyEvent` | Precursor to P7.5's single-contract law | HAS-TEETH (today's mechanic) | `dev/verify-combat-lifecycle.mjs` (11 checks per COMBAT-LIFECYCLE.md §7) | Standing |
| localStorage↔IndexedDB migration + boot hydration (precursor to P12.1/P12.2/C1F) | Precursor to the save/migration laws | HAS-TEETH (today's FOREVER-STORAGE scope, the template C1F will clone) | `dev/verify-storage.mjs`, `dev/verify-migrations.mjs`, `dev/verify-idb-hydration.mjs` | Standing |
| Soak/state-hygiene measurement instrument (precursor to P9.3/P9.6/P9.11 soak-tuned laws) | Precursor to Stage-4 cadence/hand laws | HAS-TEETH (instrument exists; not yet pointed at the new card/cadence systems) | `dev/verify-state-eval.mjs` | Standing |
| Wiring law: checks drive production entry points, re-gate greps production callers (precedent D12b/D13 already borrow) | CLAUDE.md "Disciplines" (cross-reference) | HAS-TEETH | `dev/verify-wiring-a.mjs`, `dev/verify-wiring-b.mjs`, `dev/verify-gap-wiring.mjs`, `dev/verify-gap-callers.mjs` | Standing |
| Normalization at the contract boundary (precedent for the future ActionIntent/digest schema's field coercion) | CLAUDE.md "Disciplines" (cross-reference) | HAS-TEETH (today's DM-bridge contract, the pattern to clone for new schemas) | `dev/verify-dm-contract.mjs` (`DM_EVENT_FIELDS`/`dmFoldPayload`) | Standing |
| CHANGELOG/NEXT-STEPS auto-archive discipline | CLAUDE.md "Token discipline" (cross-reference only, not a Stage-1 law) | HAS-TEETH | `build/archive-docs.py --check` | Session close |

## STAYS-PROSE (taste/judgment — Adam's capture-packet gate is the check)

| Law (short) | Source | Disposition | Check | When |
|---|---|---|---|---|
| "A human looked at the actual output (captures/trace), not just the pass count" | FEATURE-PRIORITIZATION.md §0, prototype-arrival condition 3 | STAYS-PROSE | Adam's (or the orchestrator's) capture-packet review — a machine cannot verify a human looked without contradicting the point of the rule | Every arrival declaration |
| "Personally re-gated — never a subagent's self-reported green" | FEATURE-PRIORITIZATION.md §0, prototype-arrival condition 2 (standing law) | STAYS-PROSE | Who runs the command, not what the command checks — enforced by session discipline, not a script | Every arrival declaration |
| "Adam has played or eyeballed the felt-value milestone" caps every MVP arrival | FEATURE-PRIORITIZATION.md §0, MVP-arrival condition 4 | STAYS-PROSE | Adam's taste pass | Every MVP declaration |
| No-cash ceiling ($0 marginal cost this slice, GEN-LAW-9) | SLICE-1-GATE-MATRIX.md §6 | STAYS-PROSE | Adam's own reserved spend rulings (project memory: "Adam keeps all taste/content/spend rulings") — a full accounting of "what counts as within the ceiling" is a review call, not a script | Standing |
| Gate BLOCK vs. WARN typing — the editorial act of classifying a gate | SLICE-1-GATE-MATRIX.md §2 (P12.9) | STAYS-PROSE | A one-time authoring decision at gate-definition time; once typed, each individual gate's *enforcement* is (or should be) a real check — tracked per-gate above, not here | At gate authoring |
| The DM's performance layer itself (voice, expression, choice among legal cards) — "exactly the Charter's licensed area" | P9.11 (wave-09 §18.2) | STAYS-PROSE | Only its *bounds* are checkable (never carded, no menus — tracked in NEEDS-TEETH above); the taste exercised inside those bounds is deliberately un-mechanized | Standing |
| Reopen trigger's judgment half: "is this actually a contradiction" | SLICE-1-GATE-MATRIX.md §7 | STAYS-PROSE | Fable/Adam's judgment, dated and named — the *recording* of that judgment is mechanizable (tracked in NEEDS-TEETH above), the judgment itself is not | Per contradiction |
| Agent-discipline items — "read the actual files before claiming a gap," "keep systems coherent in the same change" | CLAUDE.md "Disciplines" (cross-reference only) | STAYS-PROSE | Session/agent conduct, not a Stage-1 mechanical law; no check fits without becoming its own bureaucracy | Standing |
| Charter protected-core items #2 (narrative survives tray overflow), #3 (roll-table legibility), #7 (not dependent on finite handmade scenes), #9 (visual omission ≠ deletion), #10 (empty rooms are capacity) | GRAPHICS-CONVERGENCE-CHARTER.md §1 | STAYS-PROSE, for now, by scope | Not yet exercised at Stage-1's scale — one fixed 5×5 room, one cast, no tray/dealing/portfolio pressure exists to violate these against. No check is owed until Stage 2's tray/portfolio work gives them something to test. (#1/#4/#5/#6/#8 ARE live now and are tracked above.) | Revisit at C1H+ / Stage 2 |

---

## Summary — top gaps in build order

1. **D13 provenance audit** (no ownerless mesh) — zero teeth, the generalized fix for the exact door-stub defect class; land with the wiring-audit re-wire.
2. **Land the two uncommitted drafts** (D12a seam grid, D12b door-wiring grep-gate) already sitting in `Genesis-c1a`'s working tree — teeth are *written*, just not committed or proven green.
3. **D12b hinge geometry** — the wiring gate proves "no bespoke geometry," not "hinges correctly per THE DOOR CONTRACT."
4. **D10's "never reads/writes U"** — only half-checked (tier/frozen yes, reference-absence no).
5. **Capture rig** exists on the docs worktree, not the code worktree — the instrument that caught the door bug isn't standing infrastructure yet.
6. **Budget measurement and fixture retention** — no harness exists for either standing gate, anywhere.
7. **C1B's load-bearing trio** — TacticalQueryKernel, CheckContract failure-licenses, DC reveal-at-commit — all well-specified, all unbuilt.
8. **C1G's digest laws** (hand floor, performance-never-carded, no-coaching, fact-locked prose) — flag the hand-floor scheduling tension against Stage 4's card registry before C1G work starts.
9. **C1F's save/migration spine** has strong precedent to clone (`verify-storage.mjs`/`verify-migrations.mjs`) but nothing written yet.
10. **Machine-diffed capture goldens** — named as the graphics no-human-production mechanism but not implemented; every capture still needs a fresh human eyeball.
