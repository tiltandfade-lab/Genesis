import fs from "fs";import path from "path";import http from "http";import {createRequire} from "node:module";import {fileURLToPath} from "url";
const req=createRequire(path.join(process.env.HOME,".genesis-jsdom","package.json")),puppeteer=req("puppeteer-core");
const __dirname=path.dirname(fileURLToPath(import.meta.url)),root=path.resolve(__dirname,"../.."),mime={".html":"text/html",".js":"text/javascript",".json":"application/json",".glb":"model/gltf-binary",".png":"image/png"};
const server=http.createServer((req,res)=>{const file=path.join(root,decodeURIComponent(req.url.split('?')[0]));if(!file.startsWith(root)){res.writeHead(403);return res.end()}fs.readFile(file,(err,data)=>{if(err){res.writeHead(404);return res.end()}res.setHeader("Content-Type",mime[path.extname(file)]||"application/octet-stream");res.end(data)})});
server.listen(0,"127.0.0.1",async()=>{const browser=await puppeteer.launch({executablePath:"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome",headless:"new",args:["--use-angle=swiftshader","--enable-webgl","--ignore-gpu-blocklist","--no-sandbox","--disable-gpu-sandbox"]});try{const page=await browser.newPage();await page.setViewport({width:1440,height:1760,deviceScaleFactor:1});await page.goto(`http://127.0.0.1:${server.address().port}/dev/model-foundry/faceted-extrusion-proof.html`,{waitUntil:"networkidle0"});await page.waitForFunction(()=>window.__ready===true);const report=JSON.parse(fs.readFileSync(path.join(__dirname,"faceted-extrusion-proof/report.json"),"utf8"));await page.evaluate(data=>window.renderComparison(data),report);await page.waitForFunction(()=>window.__rendered===true);const out=path.join(__dirname,"faceted-extrusion-proof/comparison.png");await page.screenshot({path:out});console.log(out)}finally{await browser.close();server.close()}});
