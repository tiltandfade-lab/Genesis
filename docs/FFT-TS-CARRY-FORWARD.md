---
type: index
project: Genesis
status: LIVING — the one door into the FFT/Triangle Strategy evidence for the contained game
created: 2026-07-31
owner: contained-launch program
---

# FFT / TRIANGLE STRATEGY — CARRY-FORWARD INDEX

One page, one door. Every path is repo-relative from the repo root (`genesis/`), except
the two marked LANE, which live on branch `docs/spatial-compiler-research` until merge.

## Reach for these first

| doc | path | what it is |
|---|---|---|
| **The synthesis** | `Reference/FFT-Triangle-Strategy-World-Study/synthesis.md` | The master read: what FFT and TS each actually do, H1–H8 verdicts, the value-ceiling finding. The "why" document. |
| **The bands** (LANE) | `docs/intel/tactical-promise-bands.md` (+ `.json` = committed truth) | The eight fun metrics banded per size class, FFT-only and pooled side by side (ruling 2026-07-31: pooled). The "numbers a harness can hold" document. Gate spec: `docs/TACTICAL-PROMISE-GATE.md` (LANE). |
| **The corpus** | `Reference/FFT-Triangle-Strategy-World-Study/data/MEASUREMENTS.csv` | 45 maps × 37 measured columns with sources + confidence tags. Row-level authority — beats any restatement of it, including specs. |
| **The counting rules** | `Reference/FFT-Triangle-Strategy-World-Study/lane-1-footprint-topology-pacing.md` | HOW routes/chokepoints/bands/pacing were counted. The tactical gate must quote these rule-for-rule (its G3). |

## The rest of the world study (same folder)

`Reference/FFT-Triangle-Strategy-World-Study/`
- `README.md` — assignment, research-only framing, and the post-study adoption notice
  (which findings Adam authorized into the visual guide; fixed-rotation superseded by the
  four-quarter-turn ruling).
- `lane-2-geometry-shape-language.md` · `lane-3-material-texture-palette.md` ·
  `lane-4-light-atmosphere-camera-post.md` — the visual-register evidence (feeds the
  5×5/5×10 module standard and P6-era material/light work).
- `lane-5-sprite-citizenship-animation.md` · `lane-6-diorama-worldfulness.md` — figure
  and scene-life evidence.
- `lane-7-state-mutation-consequence.md` — what those games persist (spoiler: almost
  nothing; our wedge in one lane doc).
- `lane-8-production-economy-solo-translation.md` — their production math translated to
  a solo dev; pairs with the render-budget law.
- `lane-9-genesis-gap-audit.md` — where Genesis stood against all of it at study time.
- `data/MEASUREMENTS.csv` — above. `diagrams/`, `tools/`, `CORPUS-INVENTORY.md`,
  `SOURCE-LEDGER.md`, `PRIOR-FINDINGS-AUDIT.md`, `GENESIS-CAPTURE-INVENTORY.md` —
  provenance and instruments.

## The guard-post deep study (exterior-era; sleeps until re-expansion)

`Reference/FFT-Guard-Post-Study/`
- `README.md` — front door. `INVENTORY.md` + `analysis/` + `reference-index/` — the
  acquisition and per-asset analysis. `ACQUISITION.md` — how to re-fetch the 346 MB
  study-archive (gitignored local cache; the distilled docs are committed).

## Derived instruments (LANE, branch docs/spatial-compiler-research)

- `dev/research/extract-tactical-bands.mjs` — regenerates the bands from the CSV,
  deterministic (rerun before trusting any hand-quoted number).
- `docs/research/SPATIAL-COMPILER-METHOD-SWEEP.md` · `SPATIAL-COMPILER-DIVERSITY-MATH.md`
  — the method evidence and the measured replayability math built on this corpus.

**Precedence when anything disagrees:** MEASUREMENTS.csv row → lane-1 counting rule →
bands.json → any prose restatement anywhere (specs included — one already lost this
fight and carries the correction).
