---
type: system-spec
status: specced 2026-07-01 late night — the §II.0a gate plan. ONE probe (Adam's call): the HYBRID. Judged against the wired incumbent.
created: 2026-07-01
related:
  - "[[DESIGN-GUIDE]]"
  - "[[ASSET-PROMPTS]]"
---

# Style Probes — settling §II.0a with one hybrid probe

## §0. Adam's call (2026-07-01)

Probe set = **the hybrid only**: engraved chrome (frames, plaques, medallions — the wired batch-1
canon) KEPT, **scene/battle art goes low-poly FFT** (PSX-era painted-texture dimensional feel).
The incumbent needs no probe — it's already on screen; the comparison is wired-reality vs the
hybrid mock. One look, two screens, one decision.

## §1. The probe artifacts (produce, don't build)

1. **Title screen** — the GENESIS wordmark + engraved chrome as-is, with a low-poly diorama hero
   scene behind (a lone figure at a node-graph campfire — the "world rolled into being" motif).
2. **In-session screen** — the current R3 layout untouched; the scene plaque area carries a
   low-poly location vignette (the banked door-arched frame can house it); the feed/tabs/sidebar
   stay engraved.
Production route per `ASSET-PROMPTS.md` (the generation session workflow): 2–4 generated low-poly
scene pieces, composited into screenshots of the REAL current UI (no code changes — image mocks
over live captures). Both screens also rendered plain (current state) side-by-side.

## §2. The decision protocol

Adam judges A/B per screen on: does the low-poly scene art make the engraved chrome look RICHER
(jewel-box framing real art) or MISMATCHED (two games in one window)? Verdict outcomes:
- **Hybrid wins** → §II.0a resolves: engraved = chrome canon, low-poly = scene-art canon; the
  T6 battle theater + future scene art follow it; deep engraved investment UNBLOCKS for chrome.
- **Hybrid loses** → the incumbent stands whole; §II.0a resolves to engraved-everywhere; the
  low-poly question closes.
Either way the gate OPENS — the point is a decision, not a redesign.

## §3. Sequencing

An asset-generation session (Adam + image gen, ~30 min) any time — independent of all code
tracks; nothing in the overnight batch or day-2 units waits on it (COMBAT-TRACKER is
deliberately structural/asset-light so either verdict drops in).
