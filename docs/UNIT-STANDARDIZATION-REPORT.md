# Unit Standardization Report

_Generated by `build/lint-units.py` — a read-only REPORTER (never a CI gate; always exits 0). Every line is a FLAG for a human/model to judge, not a build failure._

Corpus: **510** core BESTIARY entries, **1307** realm REALM_BESTIARY entries (across 11 realms), **397** WHOLE_OBJECT_REGISTRY models, **393** NEAREST_SUB aliases.

## How to read this

**PART A — congruence** checks each bestiary entry against the field contract in `build/gen-bestiary.py` / `build/gen-realm-bestiary.py`:

- **abilities** — all six STR/DEX/CON/INT/WIS/CHA present with a real numeric score.
- **hp** — numeric hp *and* a non-empty `hpFormula` (hit dice).
- **ac** — numeric AC. · **cr** — a real CR (0 is valid; absent is not).
- **action** — at least one action/bonus/reaction/legendary, or an attack-bearing trait.
- **flavorTable** — a hand-authored d-table: core `customTables[]` (OPTIONAL by design — Adam's SACRED tables, carried on only a minority of core monsters), realm d8 `flavorTable` (expected on every realm reskin). **flavorTableDie** = a core table is present but its heading doesn't declare a die (milder note).
- **model** — the render key resolves through the wiring: EXACT (direct registry hit) / ALIAS (via NEAREST_SUB) / MISSING (drops to the cuboid fallback).
- (realm only) **required** — the 7 required fields; **frame** — `frame` resolves to a real BESTIARY id (the stat chassis a reskin inherits from). Realm STR/HP/AC/actions live on that frame, so they are not re-checked on the reskin itself.

**PART B — model standard** checks each WHOLE_OBJECT_REGISTRY entry speaks one language: a `module` that exists on disk, an `fn` actually exported by it, and a `discR` in the size-law vocabulary {0.32, 0.42, 0.48, 0.55, 0.62, 0.68, 0.72}. Plus NEAREST_SUB hygiene (no dangling alias targets).

## PART A — bestiary congruence

### Core BESTIARY (510 entries) — per-check gap counts

| check | entries flagged |
|---|---|
| abilities | 0 |
| hp | 0 |
| ac | 0 |
| cr | 0 |
| action | 0 |
| flavorTable | 406 |
| flavorTableDie | 11 |
| model | 0 |

_flavorTable is OPTIONAL for core — 104 of 510 core monsters carry a hand-authored table; the count above is coverage information, not breakage._

Model wiring: **EXACT 117 · ALIAS 393 · MISSING 0** (510 of 510 core ids render a real body; 0 fall to the cuboid fallback).

### Realm REALM_BESTIARY (1307 entries) — per-check gap counts

| check | entries flagged |
|---|---|
| required | 0 |
| frame | 0 |
| flavorTable | 0 |
| model | 0 |

Model wiring: **EXACT 734 · ALIAS 573 · MISSING 0**.

## PART B — model registry standard

| check | models flagged |
|---|---|
| module file absent | 0 |
| fn not exported by module | 0 |
| discR missing | 0 |
| discR outside size-law vocab | 0 |
| NEAREST_SUB dangling alias | 0 |

### Flagged models

- **test:grunt-glb** — no module path

## Top 20 most-incongruent entries (worst-first)

_Sort key = summed flag weight (abilities/hp/frame/required = 3, ac/cr/action = 2, flavorTable/model = 1). 417 core + 0 realm entries carry at least one flag._

| # | layer | entry | wt | flags |
|---|---|---|---|---|
| 1 | core | `aarakocra-aeromancer` | 1 | **flavorTable**: no customTables flavor d-table (optional for core) |
| 2 | core | `aarakocra-skirmisher` | 1 | **flavorTable**: no customTables flavor d-table (optional for core) |
| 3 | core | `aberrant-cultist` | 1 | **flavorTable**: no customTables flavor d-table (optional for core) |
| 4 | core | `abominable-yeti` | 1 | **flavorTable**: no customTables flavor d-table (optional for core) |
| 5 | core | `adult-black-dragon` | 1 | **flavorTable**: no customTables flavor d-table (optional for core) |
| 6 | core | `adult-blue-dragon` | 1 | **flavorTable**: no customTables flavor d-table (optional for core) |
| 7 | core | `adult-brass-dragon` | 1 | **flavorTable**: no customTables flavor d-table (optional for core) |
| 8 | core | `adult-bronze-dragon` | 1 | **flavorTable**: no customTables flavor d-table (optional for core) |
| 9 | core | `adult-copper-dragon` | 1 | **flavorTable**: no customTables flavor d-table (optional for core) |
| 10 | core | `adult-red-dragon` | 1 | **flavorTable**: no customTables flavor d-table (optional for core) |
| 11 | core | `adult-silver-dragon` | 1 | **flavorTable**: no customTables flavor d-table (optional for core) |
| 12 | core | `adult-white-dragon` | 1 | **flavorTable**: no customTables flavor d-table (optional for core) |
| 13 | core | `air-elemental` | 1 | **flavorTable**: no customTables flavor d-table (optional for core) |
| 14 | core | `allosaurus` | 1 | **flavorTable**: no customTables flavor d-table (optional for core) |
| 15 | core | `ancient-black-dragon` | 1 | **flavorTable**: no customTables flavor d-table (optional for core) |
| 16 | core | `ancient-blue-dragon` | 1 | **flavorTable**: no customTables flavor d-table (optional for core) |
| 17 | core | `ancient-brass-dragon` | 1 | **flavorTable**: no customTables flavor d-table (optional for core) |
| 18 | core | `ancient-bronze-dragon` | 1 | **flavorTable**: no customTables flavor d-table (optional for core) |
| 19 | core | `ancient-copper-dragon` | 1 | **flavorTable**: no customTables flavor d-table (optional for core) |
| 20 | core | `ancient-red-dragon` | 1 | **flavorTable**: no customTables flavor d-table (optional for core) |

