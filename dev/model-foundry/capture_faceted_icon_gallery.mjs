import fs from "node:fs";import http from "node:http";import path from "node:path";import {createRequire} from "node:module";import {fileURLToPath} from "node:url";
const __dirname=path.dirname(fileURLToPath(import.meta.url)),root=path.resolve(__dirname,"../.."),req=createRequire(path.join(process.env.HOME,".genesis-jsdom","package.json")),puppeteer=req("puppeteer-core"),mime={".html":"text/html",".js":"text/javascript",".json":"application/json",".glb":"model/gltf-binary",".png":"image/png"};
const server=http.createServer((request,response)=>{const file=path.join(root,decodeURIComponent(request.url.split("?")[0]));if(!file.startsWith(root)){response.writeHead(403);return response.end()}fs.readFile(file,(error,data)=>{if(error){response.writeHead(404);return response.end()}response.setHeader("Content-Type",mime[path.extname(file)]||"application/octet-stream");response.end(data)})});
server.listen(0,"127.0.0.1",async()=>{const browser=await puppeteer.launch({executablePath:"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome",headless:"new",args:["--use-angle=swiftshader","--enable-webgl","--ignore-gpu-blocklist","--no-sandbox","--disable-gpu-sandbox"]});try{const page=await browser.newPage();await page.setViewport({width:1440,height:1080,deviceScaleFactor:1});await page.goto(`http://127.0.0.1:${server.address().port}/dev/model-foundry/faceted-extrusion-gallery.html`,{waitUntil:"networkidle0"});await page.waitForFunction(()=>window.__ready===true);const report=JSON.parse(fs.readFileSync(path.join(__dirname,"faceted-extrusion-gallery/report.json"),"utf8"));await page.evaluate(data=>window.renderGallery(data),report);await page.waitForFunction(()=>window.__rendered===true);const output=path.join(__dirname,"faceted-extrusion-gallery/gallery.png");await page.screenshot({path:output});console.log(output)}finally{await browser.close();server.close()}});
